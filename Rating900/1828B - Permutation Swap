#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    while(t--) {
        int n;
        cin >> n;

        vector<int> p(n);
        for(int i = 0; i < n; i++) {
            cin >> p[i];
        }

        int gcd_all = 0;

        for(int i = 0; i < n; i++) {
            gcd_all = gcd(gcd_all, abs(p[i] - (i + 1)));
        }

        cout << gcd_all << "\n";
    }

    return 0;
}