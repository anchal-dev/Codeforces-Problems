#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin>>t;
    while(t--){
        long long n;
        cin>>n;
        
        long long  i =1;
        while(i<50 && n%i==0){
            i++;
        }
        
        cout<<i-1<<"\n";
        
    }
    return 0;
}